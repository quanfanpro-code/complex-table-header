---
name: complex-table-header
description: "Use when AI需要识别复杂表格本身，且存在多级表头、跨格、行列分组、嵌套、续表、表单或结构歧义等情况。"
---

# 复杂表格识别

这个 skill 帮助AI识别表格本身：先判断哪里是真正的表格，再恢复表格区块、行、列、单元格、跨格、角色和表头关系。

Excel、Word、PDF、扫描件、图片、网页或纯文本都只是证据的载体。载体可以改变可见证据，却不能替代表格结构判断。

## 成功标准

识别结果至少回答以下问题：

1. 哪里是表格，哪里不是表格。
2. 一份内容中有几个表格，每个表格有哪些内部区块；各表格和区块是独立、嵌套、并列还是续接关系。
3. 每个表格有多少行、多少列，单元格的边界和读取顺序是什么。
4. 哪些格子横跨多行或多列，哪些空白具有结构意义。
5. 每个格子的原始内容、位置、跨度和角色是什么。
6. 每个数据格受哪些列表头、行表头、分组标题、单位或注释约束。
7. 哪些结论已确认，哪些只是候选结构，冲突证据和缺失信息是什么。

完成结构识别后即停止。不要解释业务术语、推断业务含义、评价数值合理性，也不要修改原始内容。

## 核心原则

1. **表格是核心**：先识别表格对象，再考虑证据来自何处。
2. **原始结构优先**：保留原始文本、空白、顺序、坐标、跨度和样式证据；规范化结果只能另存，不能覆盖原始证据。
3. **结构先于角色**：先恢复边界、行列和跨格，再判断标题、表头、数据或注释。
4. **行列双向建模**：列表头与行表头同等重要，不能只展开列名。
5. **多证据互证**：边框、对齐、间距、重复模式、数据类型、缩进、样式、跨格和上下文应联合判断；任一单一信号都不能独自定案。
6. **不把空白都当缺失**：空白可能是跨度占位、层级留白、视觉分隔、未填写字段或提取损伤。
7. **不强求唯一答案**：证据不足时保留候选结构、置信度、冲突证据和所需补充信息，并停止强猜。
8. **不为了整齐改内容**：不得拼接、补写、改写或移动原始单元格文本来制造更漂亮的层级。
9. **调用方要求不等于证据**：即使调用方要求“只给一个干净表格”，也不能把不确定结构伪装成唯一结论。

## 快速识别主流程

严格按以下顺序工作：

1. **盘点证据**：记录原始内容、可见边界、坐标、顺序、跨格、空白、边框、对齐、缩进、字体、背景、重复和提取损伤。输入：载体提供的全部原始内容及其元信息；输出：证据清单。
2. **寻找表格候选**：找出具有二维对齐、重复字段或稳定关系的区域，不预设它一定是数据表格。输入：证据清单；输出：表格候选及其支持证据、反对证据和置信度。
3. **确定边界和类型**：提出初步边界，区分数据表格、表单式表格、排版容器、多表、嵌套表和混合结构；不能确定时保留类型候选。输入：表格候选；输出：初步边界和类型候选集合。
4. **二维递归切块**：同时检查横向和纵向分隔，细化独立表、嵌套表、并列表和续表的区块边界。输入：初步边界；输出：区块边界树及其父子、并列、续接关系。
5. **恢复无损结构**：确定行列边界、结构格、跨格关系和结构性空白，保留原始结构。输入：区块边界树；输出：无损网格（行轨、列轨、单元格、跨度、被覆盖位置和结构性空白）。
6. **识别单元格角色**：标注标题、列表头、行表头、角部表头、分组标题、数据格、单位、汇总、注释和来源。输入：无损网格；输出：每个单元格的角色或角色候选。
7. **建立表头关系**：为每个数据格建立表头关系、分组关系和作用范围。输入：无损网格和单元格角色；输出：每个数据格的表头关系链、分组关系和作用范围。
8. **处理重复与衔接**：判断重复表头、分页续表、跨区块标题和局部结构变化。输入：表头关系；输出：重复实例关系、局部结构版本和续接候选。
9. **验证并输出**：检查可逆性、覆盖范围和矛盾；输出已确认结构、候选结构、证据、冲突和待补信息。输入：以上全部结果；输出：最终识别结果（已确认结构、候选结构、支持证据、冲突证据、影响和待补信息）。

后续章节说明每一步的判断方法。

---

## 统一表格对象模型

用同一组对象表达所有复杂表格，不因载体不同而更换结构定义。

- **表格候选**：尚未确认的二维关系区域，附带支持证据、反对证据和置信度。
- **表格**：已经确认存在稳定行列关系的对象。
- **表格区块**：表格内部可独立描述的标题区、表头区、数据区、汇总区、注释区或嵌套区。
- **行轨与列轨**：逻辑网格的横向、纵向分界，不等同于视觉像素线。
- **结构格**：一个行轨与一个列轨交叉形成的最小逻辑位置。
- **单元格**：占据一个或多个结构格的内容对象。至少记录原始内容、锚点、行跨度、列跨度、边界证据、角色候选和置信度。
- **跨格关系**：一个单元格覆盖多个结构格的关系。被覆盖位置不是独立空单元格。
- **结构性空白**：虽然没有文字或数值，但承担跨度占位、层级分隔、未填写字段或视觉组织作用的结构格。
- **关系边**：记录包含、相邻、跨格覆盖、表头指向、分组归属、作用范围、嵌套和续接等关系。

始终同时保留两种视图：

- **原始结构**：保留一个单元格的原始内容和真实跨度，是可追溯的规范结果。
- **展开视图**：为了检索或逐行计算，可把跨格标签投影到其作用位置，但每个投影必须指回原单元格，不能冒充原始内容。

任何规范化文本、拼接表头或补全标签都只能放在派生字段中，不得覆盖原始结构。

## 证据与冲突规则

### 证据来源

按可靠程度优先使用：

1. 明确的行列、单元格和跨度记录。
2. 连续一致的边界。
3. 稳定的对齐、间距、缩进和重复模式。
4. 字体、背景、边框强弱等样式与视觉层级。
5. 标题措辞、单位词、汇总词、数据类型和语言语义。
6. 数值勾稽、经验阈值、模型判断或常识推断。

较低层证据只能补充较高层证据，不能在发生冲突时偷偷覆盖它。数值勾稽可以发现异常或支持候选，但不能单独证明单元格边界、层级、表头作用范围或两块内容必然属于同一张表。

### 证据记录

每个关键判断至少记录：

- 支持该判断的直接证据。
- 与该判断冲突或无法解释的证据。
- 判断是明确记录、视觉推断还是内容推断。
- 如果撤销判断，哪些下游关系会受影响。

提取结果缺列、错序、丢失边框或把跨格拆散时，把损伤本身记为证据缺口，不要把损伤后的文本序列当成真实网格。

## 表格候选与类型判断

先提出候选，再分类；不要用固定行数、固定数字占比或“看到网格就算表格”的方式裁决。

### 数据表格

存在可重复的记录轴和字段轴，多个数据格能够通过行表头、列表头或两者共同定位。允许只有一行数据，也允许没有显式边框。

### 表单式表格

以“字段标签—填写值”为主，记录模式未必逐行重复，但标签与值之间存在稳定的位置和作用关系。左右排列、上下排列和多栏表单都可成立。

### 排版容器

使用网格或对齐只是为了摆放段落、图片、签名或版面，没有稳定的行列字段关系。保留其布局信息，但不要伪造数据表头和数据格。

### 多表与嵌套表

- **独立多表**：各自有完整边界、表头或局部语境，跨边界关系弱。
- **并列表**：横向靠近但列轨、标题或表头体系各自独立。
- **嵌套表**：某个父单元格或父区块内部存在自己的行列网格；保留父子关系，不把两层网格强行摊成一层。
- **续表**：分离的区块共享同一字段体系并存在顺序衔接，但仍需记录原区块边界。

### 混合结构

同一区域含表格、标题、单位、注释、自由文本、图片或嵌套表。分别标注子区域角色和关系，不要先删除所谓“噪声”再识别。

如果数据表格、表单式表格和排版容器之间证据相当，类型保持为候选集合。

## 二维递归切块

空行只是分隔证据之一，不能自动决定边界。识别区块时同时沿横向和纵向递归检查：

1. 找出空白带、边界中断、对齐突变、列轨变化、重复表头、独立标题或独立单位等候选分隔。
2. 分别尝试横切和竖切，检查切开后的各部分是否比整体拥有更稳定的内部行列关系。
3. 只有当分隔两侧内部关系强、跨分隔关系弱时，才确认切块。
4. 对每个子块重复检查，直到继续切分不再增加结构一致性。
5. 保留父区块与子区块、原位置和读取顺序。

特别注意：

- 非空内容可以分隔两个表，空白也可能只是表内结构性空白。
- 左右并排的两个表不能因为没有空列就合成一个宽表。
- 标题、单位、来源和注释可能位于表内、表外或跨多个子表作用，位置本身不能定案。
- 两块字段相同也不必然是续表；两块字段略有变化也不必然是独立表。

## 网格、单元格与跨格恢复

1. 综合显式边界、对齐锚点、重复位置和内容流，建立候选行轨与列轨。
2. 把内容定位到结构格，记录原始顺序和边界来源。
3. 明确跨格时，只保留一个原始单元格及其跨度；其余位置标记为被覆盖。
4. 跨格仅由视觉或内容推断时，保留未合并网格作为备选，不提前复制内容。
5. 对空白结构格区分：跨格覆盖、真正空值、未填写、层级留白、分隔留白和提取缺失。
6. 检查所有单元格是否完整覆盖其声明范围，且不同单元格不存在无法解释的重叠。
7. 只有在原始结构稳定后，才生成展开视图。

跨度与重复值还要分别核对：

- 行跨度为1、列跨度大于1的是横向跨格；行跨度大于1、列跨度为1的是纵向跨格；两者都大于1的是矩形跨格。
- 明确存在但没有内容的合并区域是空白跨格，不能因锚点为空而丢弃。
- 相同文字来自不同原始单元格时是“真实重复值”，必须分别保留。
- 展开视图把同一原始单元格投影到多个结构格时是“映射重复值”，每个投影必须指回同一锚点。
- 提取造成的拆分、粘连或重复先标记为损伤候选，不能未经证据自动修复。

---

## 单元格角色

角色由单元格与其他单元格的关系决定，不能只凭“文字像表头、数字像数据”判断。一个单元格可以同时承担多个角色，证据不足时也可以保留多个角色候选。

- **标题**：命名整个表或较大表格区块。
- **列表头**：沿列方向约束下方或其他明确范围内的数据格，可形成多级父子树。
- **行表头**：沿行方向约束右方或其他明确范围内的数据格，可形成多级父子树。
- **角部表头**：位于行表头区与列表头区交会处，可能描述两个轴，也可能只是结构占位。
- **分组标题**：约束一组行、一组列、一个面板或一个嵌套区块。
- **数据格**：由行列轴或字段—值关系定位的内容格；内容可以是数字、文字、日期、符号、勾选状态、图片或空值。
- **单位**：限定数值或度量的单位、币种、比例或量纲，作用范围需单独判断。
- **小计**：汇总一个局部行组、列组或区块；其标签通常仍兼有行表头或列表头角色。
- **合计**：汇总更大范围或整表；不能仅凭“合计”二字推断表格必然在此结束。
- **注释**：补充、例外、口径或脚注内容，不默认属于表外。
- **来源**：说明表格或局部数据来源的内容。
- **结构性空白格**：承担跨度占位、字段空位、层级分隔或布局关系的无内容位置。
- **布局内容**：仅承担展示或排版作用，不建立数据关系。
- **未确定**：已有位置和原始内容，但角色证据不足；保留角色候选而不强制归类。

角色判断优先依据跨度、位置、重复模式、边界、对齐和指向关系。文字含义只能作为辅助证据。

## 表头关系

不要只输出拼接后的列名。对每个数据格建立可追溯的关系链：

1. 从外层到内层排列其列表头。
2. 从外层到内层排列其行表头。
3. 记录适用的角部表头、分组标题、单位、注释和来源。
4. 每条关系指向原始单元格标识，并记录作用范围和依据。
5. 如果一个数据格同时受多个同层表头约束，全部保留，不擅自选一个。

多级表头应表示为树或有向关系，而不是一个不可逆字符串。可以另建“外层标题｜内层标题”的展开字段，但必须保留组成它的原始表头标识。

### 表头继承规则

- 明确横跨多列的列表头可作用于其覆盖的子列。
- 明确纵跨多行的行表头可作用于其覆盖的子行。
- 缩进、边框、对齐和重复模式共同支持时，可建立层级候选。
- 空白表头只有在跨度记录或高度一致的结构模式支持时才能继承；否则标记为未解析。
- 相同文字出现在不同层级、不同位置或不同单元格时，不得仅因文字相同而去重。
- 数字、年份或序号可以是表头，文字也可以是数据，不能用内容类型直接切分表头区。

### 矩阵与双轴表

当行和列都承担分类轴时，每个数据格必须同时关联行表头路径和列表头路径。矩阵内部的对角、对称或重复数值不改变这一规则。

### 表单关系

表单式表格不必强行制造统一列名。直接记录“标签格指向值格”、分组标题指向字段组、填写区范围和读取顺序。

## 作用范围

标题、单位、分组标题、注释和来源都必须有作用范围，不能因为它们出现在表格上方或下方就自动归为表外。

范围判断依次参考：

1. 明确的包含、跨格、脚注标记或引用关系。
2. 与某个表格区块共享的边界、宽度、对齐和样式。
3. 距离、顺序、分隔和重复位置。
4. 文字含义。

单位可能作用于整表、某组列、某一列、某组行、某个数据格或多个并列表。注释也可能只约束带有标记的单元格。无法确定时，同时输出范围候选，不把它机械归入或排除出表格。

小计和合计还要记录其汇总作用范围，例如某个行组、列组、区块或整表。该范围优先依据结构边界和表头关系，数值勾稽只能辅助，不能单独确定范围。

## 重复、变化与衔接

### 重复表头

重复出现的表头可能用于分页、滚动阅读、分组重启，也可能代表新表。

- 保留每次出现的原始单元格和位置。
- 可建立“重复实例指向同一表头定义”的关系，但不能删除重复实例。
- 若重复后字段顺序、层级或单位变化，记录局部结构版本。

### 表头中途变化

表头在表格中途新增、删减、换序或改变层级时，先建立前后两个局部表头版本及各自作用范围，再判断它们属于同一张可变结构表、分组重启还是两个独立表。不得用前段表头无条件覆盖后段数据。

### 续表

以下证据共同支持续表候选：

- 行列轨、表头路径和字段顺序相容。
- 单位、标题和分组语境能够延续。
- 后一区块缺少独立标题，或明确标有延续关系。
- 前后区块的读取顺序和记录模式连续。

以下证据反对直接续接：

- 新的独立标题或新的作用范围。
- 前一区块已由合计、注释或来源完整收束。
- 表头层级、列数、字段含义或单位发生无法解释的变化。
- 两块只是左右并列或偶然使用相同字段。

证据不足时，同时保留“同一表续接”和“两个独立表”两种候选。即使选择续表，也保留原区块边界和重复表头，避免不可逆拼接。

### 跨区块断行

一行内容在视觉分隔处被截断时，只有在边界、对齐、字段缺口和读取顺序共同吻合时才建立“同一逻辑行”候选。不得仅凭句意或数值勾稽自动合并。

## 歧义与停止强猜

识别不是必须给出唯一整洁表格。出现以下任一情况时，应停止强猜：

- 无法稳定确定行列边界。
- 两种或更多网格拓扑都能解释现有证据。
- 单位、注释或分组标题的作用范围会实质改变数据格关系。
- 提取结果丢失顺序、缩进、边界或跨度，无法还原。
- 唯一化必须依赖改写原始文字、补造单元格或业务常识。
- 显式结构与视觉结构相互冲突，且无法判断哪一方损坏。

此时输出：

1. **已确认结构**：各候选都共同支持的边界、内容和关系。
2. **候选结构**：逐个说明不同的表格数量、网格、角色或作用范围。
3. **支持证据**：每个候选能够解释的直接证据。
4. **冲突证据**：每个候选无法解释或与之矛盾的证据。
5. **影响**：采用不同候选会改变哪些单元格或表头关系。
6. **所需信息**：最少需要补充的边框、坐标、原图、跨度或上下文。

置信度使用“高、中、低”或调用方要求的标尺，但必须附证据，不能只给分数。

### 🔴 CHECKPOINT：唯一化或覆盖前确认

纯只读识别且保留原始结构和候选结构时，不暂停，直接输出结果。

如果下一步会把某个候选定为唯一结构、覆盖原始结构或生成不可逆的派生结果，必须先 **🛑 STOP**：展示候选、证据、冲突和影响，等待用户确认。用户确认不能替代结构证据；没有新增证据时，只能把所选候选标为“用户指定假设”，并继续保留原始结构和其他候选。

### 防止内容诱导结构

例如只得到按行排列的文字序列：

```text
项目 2025年 2024年
货币资金 100 90
其中
银行存款 80 70
其他货币资金 20 20
单位：万元
注：以上数据未经审计
```

不能为了输出整齐而把后两项改写成带“其中”的新单元格，也不能仅凭数值相加关系宣布唯一层级。应保留原文，提出“其中是分组标题、行表头的一部分、独立说明或提取错位”等候选；单位和注释的范围也应保留候选，直到获得位置或边界证据。

## 输出要求

输出格式可以是文字、表格、树、图或结构化数据，但必须满足无损结构—内容契约。

### 必须输出

- 表格候选数量、类型、边界、读取顺序和相互关系。
- 每个表格区块的角色和范围。
- 每个单元格的唯一标识、原始内容、原始位置、行跨度、列跨度和边界依据。
- 每个单元格的角色或角色候选。
- 每个数据格的列表头路径、行表头路径、分组关系和作用范围。
- 结构性空白、被跨格覆盖位置和无法归位的内容。
- 已确认结构、候选结构、支持证据、冲突证据、置信度和待补信息。
- 原始结构与展开视图之间的映射。

### 禁止输出

- 只有拼接后的扁平列名，却无法回到原始表头。
- 为填满空位而复制内容后，把副本当成原始单元格。
- 删除重复表头、单位、注释、空白或来源而不留记录。
- 用业务解释替代结构关系，或对原始数值和文字作修正。
- 在证据不足时声称“唯一正确”“已完整还原”。

如果调用方只需要简短答案，至少给出“表格地图、关键单元格关系、不确定项”三部分；底层结果仍应遵守上述契约。

### 输出模板与完整示范

调用方需要程序可读结果时，使用以下 JSON 骨架。字段名固定，按实际填写；无法确认的字段填 null，并在候选结构中说明。

```json
{
  "表格": [
    {
      "标识": "T1",
      "类型": "数据表格",
      "类型候选": [],
      "边界": "R1C1:R4C3",
      "读取顺序": "先行后列",
      "区块": [{"标识": "B1", "角色": "表头区", "范围": "R1:R2"}],
      "单元格": [
        {"标识": "T1C01", "锚点": "R1C1", "原始内容": "一季度销售表", "行跨度": 1, "列跨度": 3, "角色": "标题", "角色候选": [], "边界依据": "明确跨度记录"}
      ],
      "关系边": [{"从": "T1C06", "到": "T1C03", "类型": "列表头约束", "作用范围": "R3C2:R3C2"}],
      "结构性空白": [{"位置": "R1C2:R1C3", "原因": "被T1C01跨格覆盖"}],
      "置信度": "高"
    }
  ],
  "候选结构": [{"说明": "", "支持证据": "", "冲突证据": "", "影响": ""}],
  "待补信息": []
}
```

以下用一张小表展示一份合规输出的最低形态。实际输出应对每个数据格逐格展开。

输入（跨度已明确）：R1C1 横跨 C1:C3，内容为“一季度销售表”；R2 依次为“地区｜销售额｜订单数”；R3 依次为“华东｜120｜30”；R4 依次为“华北｜80｜20”；表格下方另有一行“单位：万元”。

输出：

**表格地图**：1 张数据表格，边界 R1C1:R4C3，先行后列读取；R1 标题区，R2 列表头区，R3:R4 数据区；“单位：万元”位于表外下方，作用范围未定。

**单元格清单**：

| 标识 | 锚点 | 原始内容 | 行跨度 | 列跨度 | 角色 | 边界依据 |
|---|---|---|---:|---:|---|---|
| T1C01 | R1C1 | 一季度销售表 | 1 | 3 | 标题 | 明确跨度记录 |
| T1C02 | R2C1 | 地区 | 1 | 1 | 列表头 | 明确记录 |
| T1C03 | R2C2 | 销售额 | 1 | 1 | 列表头 | 明确记录 |
| T1C04 | R2C3 | 订单数 | 1 | 1 | 列表头 | 明确记录 |
| T1C05 | R3C1 | 华东 | 1 | 1 | 行表头 | 明确记录 |
| T1C06 | R3C2 | 120 | 1 | 1 | 数据格 | 明确记录 |
| T1C07 | R3C3 | 30 | 1 | 1 | 数据格 | 明确记录 |
| T1C08 | R4C1 | 华北 | 1 | 1 | 行表头 | 明确记录 |
| T1C09 | R4C2 | 80 | 1 | 1 | 数据格 | 明确记录 |
| T1C10 | R4C3 | 20 | 1 | 1 | 数据格 | 明确记录 |
| T1C11 | 表下方 | 单位：万元 | — | — | 单位 | 明确记录 |

**表头关系**：以 T1C06（120）为例——行表头 T1C05“华东”，列表头 T1C03“销售额”，另受标题 T1C01 约束；T1C07、T1C09、T1C10 按同法逐格列出。

**结构性空白**：R1C2、R1C3 被 T1C01 跨格覆盖，不是独立空单元格。

**不确定项**：单位“万元”的作用范围缺少位置与边界证据，保留两个候选——仅作用于销售额列，或作用于全部数值列；订单数是否以万元计不能在结构层裁决。

**待补信息**：单位行与表体的共享边界、对齐或样式证据。

### 大规模输入的降级输出

输入行数超过一次回复能逐格罗列的上限时（经验阈值：数据行超过约 50 行，或逐格明细将超过输出篇幅的一半），不得硬撑逐格罗列，也不得默默省略后假装完整。按以下三条降级：

1. **整体结构必须完整给出**：表格地图、表头树、跨格模式、单位与注释的作用范围，覆盖全表，不因表大而缩水。
2. **逐格明细只展开代表样本**：首行、末行、每个结构版本各一段、所有例外行。例外行（小计、合计、空行、结构变化点、跨格出现处）必须逐项列出，不得并入样本。
3. **明确记录省略范围**：写明"第 X 至 Y 行与样本 S 同构，未逐格展开"，并给出重建规则（该行区间每一行的表头关系与样本 S 完全相同，仅数据值不同）。

证据盘点和不确定项仍覆盖全表，不得只盘点样本部分。省略是输出层的取舍，不是分析层的跳过：分析必须扫过全表，确认被省略区间确实与样本同构；若做不到全表扫描（例如输入被截断），按"歧义与停止强猜"处理，明示覆盖范围。

## 复杂场景速查

| 场景 | 必须保留 | 不得直接假定 |
|---|---|---|
| 无边框或弱边框表格 | 对齐、间距、重复模式及其置信度 | 没有清晰线条就不是表 |
| 稀疏表或模板空表 | 标签、可填写区和结构性空白 | 空值多就不是表 |
| 单行或单列表 | 稳定字段关系和边界证据 | 行列少就不是表 |
| 多级行列表头 | 两个方向的表头树及跨度 | 只展开顶部列名 |
| 角部复合表头 | 单元格内子区域或多角色候选 | 一定只是装饰 |
| 对角线分隔单元格 | 分隔线、文字子区域和读取候选 | 自动拆成两个真实单元格 |
| 纵横跨格 | 原单元格、锚点、跨度和覆盖位置 | 复制后的每格都是真实内容 |
| 缩进层级 | 缩进量、同组样式和父子候选 | 缩进必然代表业务层级 |
| 小计与合计 | 汇总角色及其行列关系 | 必然结束整张表 |
| 重复表头 | 每次出现位置和重复关系 | 重复内容可以删除 |
| 分页或分块续表 | 原区块、顺序和续接候选 | 分开就是两表或必然一表 |
| 左右并列表 | 各自列轨、标题和作用范围 | 没有空列就必须合并 |
| 嵌套表 | 父单元格、子网格和父子边界 | 可以无损摊成单层表 |
| 表单式表格 | 标签—值关系和字段组 | 必须转成记录型数据表 |
| 排版容器 | 布局元素和读取顺序 | 有网格就是数据表 |
| 标题单位注释混排 | 各自原位置和范围候选 | 上方是标题、下方是表外注释 |
| 多单位混用 | 单位到行列或单元格的范围 | 一个单位适用全表 |
| 文本型数据与数字型表头 | 位置关系和重复模式 | 文字是表头、数字是数据 |
| 单元格内多值或换行 | 原始分隔和子项候选 | 换行必然代表多行或多列 |
| 符号、勾选框与图片 | 内容类型、位置和关联标签 | 非文字内容可以忽略 |
| 表头缺失 | 未命名行列和可确认关系 | 自动生成虚构表头 |
| 局部列数变化 | 区块版本、跨度或损伤候选 | 强行补齐成统一宽度 |
| 提取错序或字符损伤 | 原序列、异常位置和证据缺口 | 根据语义悄悄改回去 |
| 浮动标题或跨表注释 | 可能覆盖的多个表格候选 | 只归最近的一张表 |
| 转置或旋转布局 | 两个轴的实际关系 | 阅读方向等同于数据轴 |

## 反模式

1. 用固定比例判断表格或文本。
2. 只按空行切块，忽略纵向分隔和结构变化。
3. 看到开头单行内容就自动当标题并剥离。
4. 用“连续文字行之后出现数字”确定表头行数并设置默认值。
5. 把跨格标签复制到所有位置后丢弃原跨度。
6. 因表头文字相同就去重。
7. 把所有空白当缺失值，或把所有空白当分隔线。
8. 把单位和注释机械归为表外内容。
9. 只建立列表头，不建立行表头。
10. 用数值勾稽、语言常识或业务知识代替结构证据。
11. 为满足调用方格式要求而补写、改写或移动原始内容。
12. 把多表、嵌套表或表单强行摊成一张规则数据表。
13. 在提取损伤未解决时输出唯一结构。
14. 识别完成后继续解释业务口径、数值意义或数据对错。

## 验证清单

输出前逐项确认：

- [ ] 已区分表格、表单、排版容器、混合结构和不确定候选。
- [ ] 已同时检查横向、纵向边界以及嵌套关系。
- [ ] 行轨、列轨、结构格和跨格覆盖不存在无法解释的重叠。
- [ ] 每份派生内容都能映射回原始单元格，原文未被改写。
- [ ] 列表头和行表头都已识别，或明确标为缺失与不确定。
- [ ] 每个数据格都能追溯到适用表头，无法追溯者已单列。
- [ ] 标题、分组、单位、注释和来源均有作用范围或范围候选。
- [ ] 重复表头、续表、跨区块断行和局部变化均保留原位置。
- [ ] 结构性空白与真正空值、跨格覆盖、提取缺失已区分。
- [ ] 候选结构附有支持证据、冲突证据、影响和待补信息。
- [ ] 没有用固定阈值、默认表头行数或内容类型替代结构判断。
- [ ] 没有利用业务解释、数值合理性或调用方压力强行唯一化。
- [ ] 最终查看了完整结构结果，而不只检查中间过程。

行为验证至少覆盖四类样本：

- **正常样本**：单级表头、规则数据区和明确的标签—值表单。
- **边界样本**：单行或单列表、空白跨格、复合角部表头、多单位、嵌套表和局部表头变化。
- **错误样本**：内容错序、单元格拆分或粘连、重复提取、边界冲突和排版容器误判。
- **低置信度样本**：只有文字顺序、缺少视觉信息、疑似续表、作用范围不明和多个同等候选。

只要任何关键项无法确认，就保留不确定性并停止强猜。
